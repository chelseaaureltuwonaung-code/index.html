<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Love Letter Aesthetic</title>

<!-- Handwriting font -->
<link href="https://fonts.googleapis.com/css2?family=Pacifico&family=Great+Vibes&display=swap" rel="stylesheet">

<style>
  body {
    background: #f7eafa;
    margin: 0;
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    font-family: "Arial", sans-serif;
  }

  .envelope-wrapper {
    position: relative;
    width: 300px;
    height: 230px;
  }

  /* Envelope body */
  .envelope {
    width: 100%;
    height: 100%;
    background: #ffb3c6;
    border-radius: 8px;
    position: relative;
    overflow: hidden;
    box-shadow: 0 4px 10px rgba(0,0,0,0.15);
  }

  /* Top fold */
  .top {
    position: absolute;
    top: 0;
    width: 100%;
    height: 80%;
    background: #ff8fa3;
    clip-path: polygon(0 0, 100% 0, 50% 65%);
    transform-origin: top;
    transition: 0.6s ease;
    z-index: 3;
  }

  /* Letter */
  .letter {
    width: 85%;
    height: 160px;
    background: white;
    border-radius: 8px;
    position: absolute;
    top: 85px;
    left: 50%;
    transform: translateX(-50%);
    padding: 18px;
    line-height: 1.5;
    font-family: "Great Vibes", cursive;
    font-size: 22px;
    color: #444;
    box-shadow: 0 3px 10px rgba(0,0,0,0.1);
    transition: top 0.7s ease;
    z-index: 1;
  }

  /* Buttons */
  .btns {
    margin-top: 25px;
    display: flex;
    gap: 20px;
  }

  button {
    padding: 10px 25px;
    background: white;
    border: 2px solid #ff8fa3;
    color: #ff6f8f;
    font-size: 16px;
    border-radius: 6px;
    cursor: pointer;
    transition: 0.3s;
  }

  button:hover {
    background: #ff8fa3;
    color: white;
  }

  /* Floating hearts */
  .heart {
    position: absolute;
    font-size: 18px;
    opacity: 0;
    animation: float 2s linear infinite;
  }

  @keyframes float {
    0% { transform: translateY(0); opacity: 1; }
    100% { transform: translateY(-60px); opacity: 0; }
  }
</style>
</head>
<body>

<div class="envelope-wrapper">
  <div class="envelope">
    <div class="top" id="top"></div>

    <div class="letter" id="letter">
      you will always be my favorite<br>
      destination, my home, and my whole<br>
      heart. i love to love you.
    </div>
  </div>
</div>

<div class="btns">
  <button onclick="openLetter()">OPEN</button>
  <button onclick="closeLetter()">CLOSE</button>
</div>

<script>
function openLetter() {
  document.getElementById("top").style.transform = "rotateX(180deg)";
  document.getElementById("letter").style.top = "-10px";

  // Spawn hearts
  for (let i = 0; i < 10; i++) {
    let h = document.createElement("div");
    h.className = "heart";
    h.innerHTML = "❤";
    h.style.left = (140 + Math.random() * 40) + "px";
    h.style.top = "120px";
    h.style.animationDelay = (Math.random() * 1) + "s";
    document.body.appendChild(h);

    setTimeout(() => h.remove(), 2000);
  }
}

function closeLetter() {
  document.getElementById("top").style.transform = "rotateX(0deg)";
  document.getElementById("letter").style.top = "85px";
}
</script>

</body>
</html>
